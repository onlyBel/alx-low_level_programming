#include <stdlib.h>
#include "main.h"

/**
 * function - creates array of integers
 * @min: integer
 * @max: integer
 *
 * Return: NULL
 */
int *array_range(int min, int max)
{
	int min, int max;

	if (min > max)
		return (NULL);	
	else
		return (NULL);
}
