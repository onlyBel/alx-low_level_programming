#include <stdlib.h>
#include "main.h"

/**
 * malloc - allocates memory for an array
 * @nmemb: unsigned integer
 * @size: size of integer
 *
 * Return: NULL
 */
void *_calloc(unsigned int nmemb, unsigned int size);
{
	int size, int nmemb, calloc;

	if (nmemb / size == 0)
		return (NULL);
	else 
		return (malloc(NULL));
}
